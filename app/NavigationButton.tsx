import React, { type MouseEvent } from 'react';

function NavigationButton(props: { buttonHoverImage: any; buttonText: any; }) {
  let buttonHoverImage = props.buttonHoverImage
  let buttonText = props.buttonText

  // return <button onMouseOver={}>{buttonText}</button>;
};

export default NavigationButton;